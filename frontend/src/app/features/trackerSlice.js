import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    length: 0,
    list: [],
};

export const trackerSlice = createSlice({
    name: 'tracker',
    initialState,
    reducers: {

    }
})